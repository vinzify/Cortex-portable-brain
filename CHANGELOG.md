# Changelog

## 0.1.0-alpha.1
- Initial standalone `cortex-brain` split from monorepo portable workspace.
- Added git-pinned RMVM core dependencies with `core_version.lock`.
- Added CI gates for core lock, proto checksums, and dependency manifest hash.
- Added release pipeline for multi-OS binaries, checksums, signatures, and SBOMs.
- Added Docker publish workflow and installer scripts.
- Added migration, operations, security, and forget UX docs.
